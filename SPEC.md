# Braid
status: approved
owner: Alexandro
one-liner: A native macOS menu-bar app that records calls as two Tracks, transcribes and identifies speakers on-device, summarises on-device, and writes markdown Notes into an Obsidian Vault. Nothing it hears ever leaves the Mac.

## Outcome
Problem: meeting knowledge evaporates after calls; manual note-taking during them is lossy and distracting; and the people you meet weekly arrive as anonymous "Speaker 1" every single time.
User: the owner, on an 8GB M3 MacBook Air already strained by Teams.
Success measure (all owner-verified, all can fail):
1. With Immediate Delivery, a Note appears in the Vault within 15 minutes of Stop with zero manual steps, for ≥9 of the first 10 real calls.
2. After 2 weeks of use, the owner has stopped taking manual notes during calls.
3. A Person named once is recognised on return: across the first month, at least 7 of every 10 returning Speakers with ≥2 minutes of speech are auto-named, and **every** auto-assigned name is correct — one wrong auto-name fails this measure outright.

## The Journey
1. One-time setup: choose the Vault folder, pick which Preset shapes every Note, maintain the Key Terms list. No accounts, no API keys. Selecting the Parakeet Engine later downloads its models; everything else is OS-provided.
2. Call starts → click the menu-bar icon → Start recording, one click. The panel offers only who is on the call — a chip per Person in the Voice Database, plus a field for anyone new — and an optional asserted speaker count (with a strict variant that also caps it). No Preset and no Title are asked for: the Preset is a Settings choice, and the Note titles itself from the summary (R9a). Recording begins: two Tracks, crash-safe on disk, continuously flushed.
3. During the call: the icon shows recording state; Pause/Resume any time; a pause stops both Tracks on the shared clock and is marked in the Transcript, never silently joined. No inference of any kind runs while any Session is recording.
4. Call ends → Stop. A Job is created and runs entirely on this Mac.
5. The Job transcribes each Track with the chosen Engine (Mic Track is "Me" structurally, no diarization) and diarizes the Remote Track. Each Speaker's representative embedding is matched against the Voice Database: confident matches are auto-named; plausible matches become suggestions; Speakers matching Me's Voiceprint are folded as echo. Re-attribution then corrects stretches the Voiceprints confidently contradict.
6. For every Speaker still unnamed, a Voice Clip is extracted before anything is deleted. If Delivery is Held and unnamed Speakers remain, the Job pauses before summarising and waits indefinitely, visibly pending in the panel. With Immediate Delivery (default), processing continues with generic labels.
7. The Summariser produces the Note on-device from the Transcript using the Preset; long Transcripts are summarised map-reduce. The Note is written to the Vault, the Transcript to `<Vault>/transcripts/`; the Recording is deleted only after both are confirmed on disk.
8. macOS notification: "Note ready", and "N voices to name" when unnamed Speakers exist. The naming flow, in the one window: each unnamed Speaker with a playable Voice Clip, how long they spoke, suggestion chips (voice matches first, then Participants, then recent Persons), a free text field, and Skip. Naming relabels the Transcript, re-summarises, rewrites Note and Transcript in place (R6a), and enrolls the confirmed Voiceprint; the Session's Voice Clips are deleted once its Identification resolves.
9. On any failure the Recording is kept and the icon shows the error state. A Job blocked by a temporary local condition (a Session recording, Engine assets not yet installed) waits and resumes by itself; anything else parks for user-initiated Retry.

## Non-goals
1. No live transcription or scrollback UI.
2. No auto-detection of call start; Start is always manual (auto-stop per R17).
3. No calendar, EventKit, Graph, or Teams integration — ever. Identity comes from voices and the owner's naming, never from an external source.
4. No cloud services: no STT API, no LLM API, no telemetry, no sync. Network I/O exists only to fetch Engine model assets ([ADR-0006](docs/adr/0006-zero-cloud.md)).
5. No inference of any kind while recording; R4's in-call budget is what the 8GB constraint protects.
6. No true clusterer seeding in this version: known voices are never fed into the diarizer's clustering (Re-attribution corrects its output instead); recorded as a future direction pending upstream support.
7. No distribution: personal build, one Mac, no notarization, no App Store.
8. No in-app transcript or note viewer/editor; Notes are read and fixed in Obsidian.
9. No RAG or chat over Notes.

## Language
The project's language lives in [CONTEXT.md](./CONTEXT.md); its terms are binding. To read this spec you need: **Session** (one Start→Stop span), **Track** (Mic = "Me", Remote = everyone else), **Speaker** (one distinct voice in a Session's Remote Track), **Person** / **Voiceprint** / **Voice Database** (a durable named identity / one stored exemplar of its voice / the encrypted store of them), **Identification** / **Enrollment** (resolving a Speaker to a Person / the learning that naming causes), **Engine** (an on-device transcription implementation), **Delivery** (Immediate or Held), **Job** (the post-Stop pipeline).

## Requirements
R1. Mic and system audio are captured as two separate Tracks via Core Audio process taps; the app never holds the Screen Recording permission. | Check: no ScreenCaptureKit reference in the codebase; a test Session produces two CAF files, each containing non-silent audio.
R2. A Recording survives a crash: `kill -9` mid-Session leaves both Track files playable, missing ≤5 seconds. | Check: kill the app during a test recording; both files play and their duration is within 5s of the kill time.
R3. Pause excludes audio and marks the gap: words spoken while paused never appear in the Transcript; the Transcript contains a marker line `[recording paused — <duration>]` at the pause point. | Check: speak a known phrase during Pause; assert it is absent and the marker line present with a plausible duration.
R4. In-call footprint is ≤5% CPU (as reported by `top` %CPU, where 100% = one core) and ≤100MB RAM while recording. | Check: sample with `top` during a 10-minute recording while a video call or video playback runs with the mic live.
R5 (amended 2026-08-02, zero-cloud). With Immediate Delivery, a Note and its Transcript appear in the Vault within 15 minutes of Stop with zero user action, and delivery posts the "Note ready" notification; a Held Session with no unnamed Speakers behaves identically. The 15-minute promise does not apply to a Held Session awaiting Identification. | Check: scripted end-to-end run against a sample Recording, asserting the notification was requested.
R6 (amended 2026-08-02, zero-cloud; original cloud request contract retired). The Start assertions bind the local pipeline: Key Terms with the Session's Participants appended (deduplicated case-insensitively) go to Engines that accept them; an asserted speaker count constrains the diarizer's clustering as a minimum, with the maximum added only when the user chose strict; a constraint is never derived from Participants. The Mic Track is never split into more than one speaker; the single-speaker embedding pass R28 needs is pinned to exactly one voice, so it structurally cannot split it. | Check: `engineReceivesKeyTermsAndDiarizerTheAssertedCount`, `micTrackIsNeverDiarized`, `learningMeAsksForExactlyOneSpeaker`.
R6a. Remote Speakers can be named after delivery. A Session whose Transcript contains Remote Speakers keeps a NamingRecord; naming relabels the Transcript, re-runs the Summariser, and rewrites the Note and Transcript in place. One case is named *before* delivery: exactly one declared Participant and exactly one heard voice is unambiguous, so the pipeline relabels that voice pre-summary — unless a confident Voiceprint match names a *different* Person, in which case neither is applied and the Speaker goes to the naming flow. Names are never assigned by guessing *among* voices. The Note is never rewritten if it has changed on disk since delivery — a new pair is written instead. | Check: `renamingAppliesNamesButNeverTouchesMe`, `transcriptStoreRoundTripsAndPurges`, `overwriteKeepsBothFilenamesAndRelinksTranscript`, `autoAssignNamesTheSingleVoiceBeforeSummarising`, `autoAssignNeverGuessesAmongVoices`.
R7. A Recording is deleted only after both its Note and its Transcript are confirmed on disk; any Job failure retains the Recording and surfaces the error state; non-transient failures are retried only on user-initiated Retry. | Check: make the Vault unwritable → Recording survives, error state shows, no automatic retry occurs; restore and Retry → Job succeeds and the Recording is deleted.
R8 (amended 2026-08-02, zero-cloud). A Job blocked by a temporary local condition — a Session currently recording, or Engine assets not yet installed — waits and completes without interaction once the condition clears. All other failures park per R7. | Check: `localWorkWaitsWhileASessionIsRecording`.
R9. Note filename is `YYYY-MM-DD HHmm Title.md` — date and time are the Session's Start in local time; Title comes from R9a with the characters `/ \ : # ^ [ ] |` replaced by `-`; on collision, append the lowest free integer suffix starting at 2, space-separated (" 2", " 3", …). | Check: force two collisions; three files exist with correct names.

R9a (2026-08-03). The Note titles itself. Nothing is asked for at Start; a Session begins as the time of day ("2:15pm recording") and the Summariser returns a title alongside the Note body, which becomes the Session's title and therefore both filenames. A title is applied only while a Session still carries the automatic one, so a Session already delivered under a name is never silently renamed by a later pass; a title that is empty, too short, too long, or otherwise unusable is rejected and the time-of-day stand-in is kept. The Summariser is never told the title it is replacing. | Check: `theSummarysTitleBecomesTheNotesName`, `aTitleTheOwnerChoseIsNeverOverwritten`, `aSessionWithNoUsableTitleKeepsItsPlaceholder`, `theSummariserIsNotToldTheTitleItIsSupposedToWrite`, `aModelsTitleIsTidiedOrRejected`, `aTitleIsReadOutOfAnOpenModelsReply`.
R10 (amended 2026-08-02, zero-cloud). Note frontmatter carries exactly these YAML keys: `date` (yyyy-MM-dd), `start` (HH:mm, local), `duration` (HH:MM:SS of recorded audio, pauses excluded), `preset`, `participants` (inline list), `engine`, `transcript` (a quoted wikilink to its Transcript). The cost and provider fields are gone: nothing is metered. | Check: parse a generated Note's frontmatter and assert that exact key set and those formats.
R11 (amended 2026-08-02, zero-cloud). The Summariser runs entirely on-device, and the user chooses which model writes the Note: **Apple's built-in** (default, weightless) or an **open-weights model** through MLX. Both produce the same Note shape — an opening summary plus one section per heading the Preset names, assembled by Braid — and both return a title for it (R9a), which is read out of the reply and never rendered into the body. Apple's is constrained by a generation schema; the open model is asked for JSON and its reply is recovered leniently, because raw JSON must never reach a Note. Apple's model refuses some subjects outright and no setting reaches that: a refusal is recovered by summarising the parts it will accept and stating in the Note how many parts are missing, never by failing the Job. A Transcript exceeding the model's context window is summarised map-reduce: chunk summaries, then a combine pass; content from every part of the Session can reach the Note. Every Preset template embeds this canonical rule verbatim: "Only attribute a name to a speaker when the transcript itself provides evidence for it; otherwise keep the generic speaker label. Never relabel Me." | Check: assert all four shipped templates contain that exact sentence; `aCleanJSONReplyBecomesANote`, `theWrappingAroundAReplyIsStripped`, `malformedJSONIsSalvagedRatherThanShown`, `prosePassesThroughUnchanged`, `anEmptySectionListStillYieldsTheSummary`; and a fixture Transcript longer than one context window yields a Note referencing content from both its first and last ten minutes.
R12 (amended 2026-08-03). Four editable Presets ship: Meeting, Lecture, Interview, Training, seeded from repo-defined defaults; the Key Terms list is likewise user-editable in Settings. One Preset is chosen in Settings and shapes every Session — the Start form no longer offers the choice, because which headings a Note carries is a standing preference rather than a per-call decision, and a Vault is searchable precisely because every note files things the same way. | Check: settings lists all four and the Key Terms editor; an edit to either persists across relaunch; the chosen Preset survives being deleted or renamed by falling back to the first.
R13 (amended 2026-08-02, zero-cloud). Braid holds no API keys and no accounts. The only Keychain item is the Voice Database key, never synced to iCloud, and readable without a password prompt so that rebuilding the app does not interrupt the user — [ADR-0007](docs/adr/0007-the-voice-database.md) records why that trade costs nothing this design was claiming. | Check: grep the app's files, defaults, and logs for key material; none found outside the Keychain.
R14 (retired 2026-08-02, zero-cloud). Cost accounting is gone: no rate table, no spend total, no usage cap. Nothing the app does has a per-use price.
R15. The app is menu-bar only (no dock icon), launches at login, and shows distinct idle / recording / paused / processing / error states. | Check: `LSUIElement` is set; the login item is registered; each state can be driven and observed.
R16. A completed Session whose Remote Track peaks below −50 dBFS throughout triggers a "no system audio captured — check permission" warning; the Job still completes and produces the Note from what was captured. | Check: run a Job on a fixture Session whose Remote Track is all-zero samples; the warning appears and a Note is still written.
R17. Starting is always manual; stopping may be automatic. A call app is any process whose bundle ID prefix-matches the user-editable watch list in Settings (seeded with Teams, Zoom, Slack, Webex, FaceTime, Chrome, Safari and peers). When a watched call app releases the microphone, the Session stops by itself after a 30-second countdown the user can cancel. The watcher arms only after a call app has held the mic during that Session, and requires the mic released continuously for 15 seconds. Cancelling disarms it until a new call begins. | Check: `CallWatcher` tests — never fires unarmed, fires once after the grace period, rides out a reconnect, and stays disarmed after Keep recording.
R18 (amended 2026-08-02, zero-cloud). Delivered Sessions are logged so the app can show its own history: a panel view listing the newest Sessions (index capped at 200), each opening its Note. The spend and cap machinery is retired with R14; recorded minutes remain visible in Settings. | Check: `sessionIndexKeepsNewestFirstAndTrimsTheTail`, `usageCountsThisMonthOnly`.
R19. A Job can be cancelled before it finishes. Cancelling keeps the Recording and is never reported as a failure — it does not auto-retry and does not park as `.failed`. A cancelled Job can be processed after all, or its Recording deleted on explicit confirmation. | Check: `cancellingAQueuedJobNeverStartsTranscription`, `cancellingLeavesTheRecordingUntilDiscardIsAsked`, `cancellingIsNotAFailure`, `cancellationIsClassifiedAsCancellationNotFailure`.
R20 (amended 2026-08-02, zero-cloud). All transcription and diarization happen on this Mac. Two Engines ship — Apple SpeechTranscriber (default) and Parakeet, selectable in Settings; diarization is the offline pyannote pipeline on the Remote Track only. No code path can send audio, Transcript text, or anything derived from them off the machine; the app's only network use is fetching Engine model assets. | Check: grep finds no networking client outside the model-asset modules; the Done gate runs the whole journey with Wi-Fi off.
R21 (amended 2026-08-02, [ADR-0007](docs/adr/0007-the-voice-database.md) superseding ADR-0003). Only vetted Voiceprints of user-confirmed Persons persist. Per-chunk embeddings die with the Job that produced them. One exception is named rather than hidden: a Speaker still awaiting a name leaves its candidate centroid in that Session's naming record, because naming it later is what turns it into a Voiceprint — it lives exactly as long as the Voice Clip beside it and dies with it when Identification resolves. Everything that outlives a Job — the Voice Database and the naming records — is encrypted with one key held in the login Keychain and never synced, so a copy of either file away from this Mac is unreadable. No other audio-derived artifact survives outside the Vault. | Check: `noRawEmbeddingSurvivesACompletedJob` (scan of persisted files for readable voice data), `theVoiceDatabaseIsNotPlaintextOnDisk`, `namingRecordsAreNotPlaintextOnDisk`, `skippingResolvesIdentificationAndDeletesClips`.
R22. Speaker separation on the Remote Track attributes each word to whoever was speaking, using the shared Recording clock: a word is assigned by its midpoint, snapped to the nearest turn when it falls in a diarizer gap, and never dropped. One Speaker's consecutive words become one Utterance, split when the silence between them exceeds the turn gap (1.5 seconds). An Engine that returns text without timings yields one honest unattributed block rather than invented positions. | Check: `alignerPutsEachWordWithWhoeverWasTalking`, `alignerAssignsAStraddlingWordByItsMidpoint`, `alignerNeverDropsWordsThatFallBetweenTurns`, `alignerSplitsOneSpeakerOnALongSilence`, `pipelineFallsBackToOneBlockWithoutTimings`.
R23 (2026-08-02). Identification is precision-first. A Speaker is auto-named only when its match to exactly one Person clears the auto threshold; between the suggest and auto thresholds it yields a suggestion chip, never a name; below, it is unknown. A tie between Persons never auto-names. Both thresholds live in one config and are calibrated with the measurement harness. | Check: `aVoiceThatMatchesOnePersonWellIsNamedWithoutAsking`, `aNearMissSuggestsRatherThanNames`, `aTieBetweenTwoPeopleNeverAutoNames`, `anUnknownVoiceStaysUnknown`; and `--local-check` reports, for every voice in a real recording, what the thresholds would decide and how much room they have above the worst different-voice similarity in that same recording.
R24 (2026-08-02). Naming a Speaker enrolls its representative embedding as a Voiceprint of that Person, capped at 10 per Person with the oldest evicted (the cap lives in the thresholds config); correcting a wrong suggestion or auto-name removes the Voiceprint that caused it. A Speaker with less than the configured minimum of speech is named in the Transcript but teaches nothing. Enrollment happens only through Identification — the sole other write path into the Voice Database is Me's structural enrollment (R28). | Check: `namingEnrollsAVoiceprint`, `aVoiceWithTooLittleSpeechIsNotEnrolled`, `theCapEvictsTheOldestVoiceprint`, `correctingAWrongNameRemovesTheOffendingVoiceprint`, `correctingAnAutoNamedVoiceUnlearnsTheVoiceprintBehindIt`.
R25 (2026-08-02). For every Speaker neither confidently auto-named nor echo-folded (R28), a Voice Clip of 5–8 seconds is cut from that Speaker's longest overlap-free turn before the Recording is deleted, playable in the naming flow, and deleted when the Session's Identification resolves: every such Speaker named or skipped (Skip is per-Speaker; skipping the last one resolves the Session). Unnamed Speakers are announced with an "N voices to name" notification at delivery, or at the hold when Held. | Check: `clipsComeFromTheSpeakersLongestOverlapFreeTurn`, `aSpeakerWithOnlyFragmentsGetsNoClip`, `skippingResolvesIdentificationAndDeletesClips`.
R26 (2026-08-02). Delivery is a single Settings toggle, Immediate by default. A Held Session with unnamed Speakers pauses before summarising and waits indefinitely, visibly pending in the panel, its Recording retained (R7); naming or skipping releases it. A Held Session with every Speaker auto-named never waits. | Check: `heldDeliveryWaitsForNaming`, `heldDeliveryWithEverySpeakerMatchedDeliversImmediately`, `namingReleasesAHeldJob`.
R27 (2026-08-02). Re-attribution corrects the diarizer with the Voice Database: a chunk assigned to one Speaker whose embedding matches a different Person above the auto threshold, and fails to match its own assignment, is re-attributed before the Transcript is assembled. When the assigned Speaker has no enrolled Person, "fails to match" means the chunk also sits closer to the other Person than to its own cluster's centroid by the configured margin. Corrections are logged per Job. Re-attribution never runs below the auto threshold and never invents Persons. | Check: `reattributionMovesAContaminatedTurnToThePersonItSoundsLike`, `reattributionNeverFiresBelowTheAutoThreshold`, `reattributionIsANoOpWithoutKnownVoices`.
R28 (2026-08-02). Me's Voiceprint is enrolled automatically from each Session's Mic Track — the one structurally known voice, the sole exception to R24's Identification-only rule, under the same cap and model-version rules — and exists solely for echo: a Remote Speaker matching Me above the auto threshold is relabelled "Me (echo)" in the Transcript — never deleted — counts as resolved (no Voice Clip, no prompt, never blocks Held Delivery), and Me is never offered as a suggestion for any other Speaker. | Check: `echoOfMeIsFoldedNotDeleted` (which also asserts the folded voice gets no clip and never holds delivery), `meIsNeverASuggestion`.
R29 (2026-08-02). The Voice Database is the user's to manage: Settings lists every Person with rename and forget (deleting all their Voiceprints); the whole database can be deleted with confirmation; export writes a readable file only on explicit action, and import restores an export. None of these touch existing Notes or Transcripts. | Check: `forgettingAPersonRemovesAllTheirVoiceprints`, `exportThenImportRoundTrips`, `importingADatabaseFromAnotherModelIsRefused`.
R30 (2026-08-02). The Voice Database is stamped with the embedding model version. On mismatch, every Voiceprint is stale: matching and Re-attribution are disabled, Identification prompts resume as if the Persons were unknown, and ordinary naming re-enrolls them under the new version. Stale Voiceprints are never matched against. | Check: `aForgedModelVersionDisablesMatchingUntilReenrollment`, which also asserts that naming someone again keeps their name, drops the stale exemplars, and restores matching.

## Design
Native, minimal, no web frontend of any kind. AppKit owns the status item and the one window; SwiftUI draws its content, with no `@State` anywhere (ADR-0004).

**One window.** The app never puts a second thing on screen. The Sessions panel hangs from the menu bar icon, and every part of the app is a view inside it: Session history, the live recording block, naming, settings, and confirmations. Left click opens and closes it; right click gives shortcuts plus Quit. macOS notifications are the only surface while the panel is closed. The panel is dark regardless of system appearance.

**Narrow.** The panel is 228 points wide (2026-08-03, was 380). It drops out of the menu bar over whatever you are doing, and everything in it is a short line or a chip, so the width was mostly padding. Nothing inside may assume two controls fit side by side: chips wrap, actions sit under the row they belong to, and the recording controls stack.

**The start form is the panel.** Opening it shows what a recording needs — who is on the call — and one Start recording button. There is no reveal step: a Record button that only opened a form made starting a recording two clicks where one would do.

**The naming flow** is one panel view: one row per unnamed Speaker — a play button for its Voice Clip, how long they spoke ("14 min, 32 turns"), suggestion chips in order (voice match "Sounds like Sarah?", then that Session's Participants, then recently named Persons), a free text field, and Skip. Naming is one tap on a chip or a few keystrokes.

**Settings** is one visible setting and five drawers. The Vault folder stays in the open, being the only thing Braid cannot work out for itself; Notes (Preset, Delivery toggle, prompt editor), Voices (the Persons list — rename / forget / delete all / export / import), Models (both Engine pickers, Key Terms) and Recording (auto-stop, call-app watch list) are collapsed until asked for. Each control carries at most one line of explanation; anything longer belongs in a tooltip or in these documents. The month's minutes moved to History, being something to look at rather than to set.

Concurrency: one Session at a time; Jobs run in the background, so Start is always available. Icon precedence: recording/paused > error > processing > idle.

## Architecture
Swift, menu-bar-only (`LSUIElement`), SwiftPM + Command Line Tools, macOS 27, Swift Concurrency throughout ([ADR-0004](docs/adr/0004-swiftpm-no-xcode-toolchain.md)). One third-party dependency: FluidAudio, pinned exactly — the sole exception to ADR-0004's zero-dependency policy, unchanged by this revision.

**Capture** ([ADR-0001](docs/adr/0001-process-tap-two-track-capture.md)): Core Audio process tap + physical mic in one aggregate device with drift compensation. Each Track is a mono CAF, 16kHz 16-bit PCM, continuously flushed. No transcode exists: Engines read the CAF originals.

**Transcription** ([ADR-0005](docs/adr/0005-local-transcription-as-a-provider.md), [ADR-0006](docs/adr/0006-zero-cloud.md)): two Engines behind the `TranscriberEngine` protocol — Apple `SpeechTranscriber` (default: better measured attribution, Key Terms as `contextualStrings`, no download) and Parakeet TDT via FluidAudio (better WER, models cached under Application Support). Diarization: FluidAudio's offline pyannote community-1 pipeline, Remote Track only. `SpeakerAligner` (R22) then the merge rule, both unchanged.

**Identification**: the diarizer result's per-cluster centroids are matched against Voiceprints by cosine similarity; two thresholds (suggest, auto), the re-attribution margin, and the Voiceprint cap live in one config file, calibrated via the harness. Re-attribution (R27) uses the pipeline's per-chunk embeddings (`exposeChunkEmbeddings`) to correct cluster contamination against enrolled Persons. Raw embeddings never cross the Job boundary (R21). **The measurement harness** is the app binary's `--local-check <audio> [--reference <json>] [--engine ...]` mode: it scores WER, coverage, turn purity, and identification precision/recall against labelled fixtures in `test-audio/` (real recordings, gitignored), printing aggregates and short excerpts only, never writing a transcript to disk.

**Voice Database**: one file under `~/Library/Application Support/Braid/`, ChaChaPoly-encrypted (CryptoKit), key in the login Keychain, never synced, with a permissive ACL so access never prompts (ADR-0007). Inside: schema version, embedding model version, Persons `{id, name, voiceprints: [{vector, heardAt, seconds}]}`. Dozens of Persons, kilobytes each; no database engine.

**Summarisation**: two engines behind `NoteSummarising`, chosen in Settings. **Apple Foundation Models** on-device (default), guided generation emitting an opening summary plus one section per Preset heading, map-reduce beyond its ~4k context; schemas are built by hand with `DynamicGenerationSchema` rather than the `@Generable` macro, which suits user-editable Presets better since the Note's shape is built at runtime. **MLX** (`BraidMLX`, Qwen3 4B by default) for sessions Apple refuses on subject; 32k context, so an hour fits in one pass. Its reply is recovered by `ModelReply`, which tolerates code fences, reasoning blocks, echoed titles and malformed JSON. The MLX dependency sits at the edge in its own target: BraidCore stays buildable and testable without it, which is what keeps `swift test` working when `swift build` cannot compile Metal shaders. The Summariser sits behind a protocol seam so a stronger local engine can replace it on evidence. Preset defaults live in the repo, seeded at first launch, user-editable; all four embed the R11 canonical rule.

**Merge rule**: both Tracks share the Recording clock; the Transcript interleaves both utterance lists by start timestamp. Remote Speakers numbered by first appearance; every Mic utterance is "Me"; pause markers at their clock position.

**Transcript format**: markdown, one line per utterance: `- **HH:MM:SS Speaker:** text`, timestamps in elapsed recorded-audio time since Start (the clock pauses with the Recording); pause markers as their own line.

**Vault delivery**: atomic write (temp file, rename). Transcript at `<Vault>/transcripts/<Note name> (transcript).md`, linked from the Note's frontmatter as a wikilink.

**State**: Job state and pending Recordings in `~/Library/Application Support/Braid/`; Voice Clips under it too, transient per R25. Signing: the stable self-signed identity "ms-notes Development" (ADR-0004; renaming it makes TCC forget granted permissions).

## Operation
No server, no accounts, no spend. Production = the signed `.app` in `/Applications`, launched at login. Deploy = build script → copy; rollback = reinstall the previous version's kept zip; one git tag per installed version. Logging = unified logging (`os_log`); the pipeline category records each Job step, including per-Job Re-attribution corrections; every failure also surfaces in the menu-bar error state. Model assets: Apple speech and foundation-model assets are OS-owned; Parakeet models (443MB) are fetched only if that Engine is selected. Backups: Notes and Transcripts live in the Vault under its existing backup; the Voice Database is included in Time Machine as an encrypted blob that is useless without this Mac's Keychain — export (R29) is the deliberate cross-machine path; Recordings are transient by design. Data protection: nothing derived from audio leaves the machine, ever (R20, R21); deletion rights are first-class (R29). Consent posture: the owner verbally mentions recording; local-only processing and audio auto-deletion are the mitigations.

## Done
Owner-executed (the R-checks are agent-runnable; this gate is human). On the owner's Mac, in production, **with Wi-Fi off**: record one real call of ≥15 minutes with ≥2 remote speakers, using Pause once and entering Participants. Within 15 minutes of Stop, hands-off: a Note is in the Vault with the pause marked, no name guessed, frontmatter complete, Transcript written and linked, Recording deleted. Name the Speakers through the naming flow, hearing each Voice Clip; the Note is rewritten named and the clips are gone. Then, within the following week, a Session containing one of those named Persons auto-names them with zero interaction — and no Session in the first month ever auto-names anyone wrongly. Then the rolling criterion: ≥9 of the first 10 real calls deliver their Note hands-off within 15 minutes. Anything less fails the spec.
